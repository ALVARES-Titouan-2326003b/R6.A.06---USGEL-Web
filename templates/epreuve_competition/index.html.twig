{% extends 'base.html.twig' %}

{% block title %}EpreuveCompetition index{% endblock %}

{% block body %}
    <h1>EpreuveCompetition index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for epreuve_competition in epreuve_competitions %}
            <tr>
                <td>
                    <a href="{{ path('app_epreuve_competition_show', {'competition': epreuve_competition.competition}) }}">show</a>
                    <a href="{{ path('app_epreuve_competition_edit', {'competition': epreuve_competition.competition}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="1">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_epreuve_competition_new') }}">Create new</a>
{% endblock %}
