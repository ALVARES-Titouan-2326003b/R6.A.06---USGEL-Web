<form method="post" action="{{ path('app_epreuve_competition_delete', {'competition': epreuve_competition.competition}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ epreuve_competition.competition) }}">
    <button class="btn">Delete</button>
</form>
