{% extends 'base.html.twig' %}

{% block title %}NomSport{% endblock %}

{% block body %}
    <h1>NomSport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nom_sport.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ nom_sport.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_nom_sport_index') }}">back to list</a>

    <a href="{{ path('app_nom_sport_edit', {'id': nom_sport.id}) }}">edit</a>

    {{ include('nom_sport/_delete_form.html.twig') }}
{% endblock %}
